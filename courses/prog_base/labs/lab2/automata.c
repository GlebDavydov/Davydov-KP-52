#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int run(int moves[], int movesLen, int res[], int resLen){
	//alphabet = 3, 5, 12, 102;
	int machine[movesLen][6];
	int	machLen = fmax(movesLen, resLen);
	for(int i = 0; i < movesLen; i++)
		
}